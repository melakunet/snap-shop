<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- Covers localhost and .local for Simulator runs -->
		<key>NSAllowsLocalNetworking</key>
		<true/>
		<!-- DEV ONLY: allows plain HTTP to the LAN IP used by wrangler dev on the Mac.
		     Real devices on the same Wi-Fi network need this because NSAllowsLocalNetworking
		     does NOT cover numeric RFC-1918 addresses (192.168.x.x).
		     REMOVE this entire NSExceptionDomains block before production / App Store submission
		     and switch AppConfig.backendBaseURL to the production HTTPS URL. -->
		<key>NSExceptionDomains</key>
		<dict>
			<key>192.168.2.12</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
			</dict>
		</dict>
	</dict>
	<key>NSMicrophoneUsageDescription</key>
	<string>Snap&amp;Shop uses the microphone when recording video in Deep Scan mode.</string>
</dict>
</plist>
